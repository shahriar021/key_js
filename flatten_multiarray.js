let smiles = [ [':)',':D'], [':(',':O'] ]
console.log(smiles.flat())
//explantion:
//The flat() method creates a new array with all sub-array elements concatenated into it recursively up to the specified depth.